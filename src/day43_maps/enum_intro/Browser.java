package day43_maps.enum_intro;

public enum Browser {
    /*
    enum ---> Enumeration
    It's a special datatype that is MOSTLY used to define the CONSTANTS
    Usually written in UPPERCASE
    They (enums) act like classes, they can have variables (MOSTLY) and methods
     */
    CHROME, FIREFOX, SAFARI, EDGE, OPERA
}